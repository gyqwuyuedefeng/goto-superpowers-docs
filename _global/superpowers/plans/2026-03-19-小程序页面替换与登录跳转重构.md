# 小程序页面替换与登录跳转重构实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 用 pages/home 的功能列表页面完全替换 pages/index 的欢迎页面，并更新登录跳转逻辑

**架构：** 使用 Git 重命名方式，删除旧的 pages/index 目录，将 pages/home 重命名为 pages/index，然后更新所有配置文件和引用

**技术栈：** 微信小程序、Git

---

## 文件结构映射

**将被删除的文件：**
- `goto-wechat/pages/index/index.js`
- `goto-wechat/pages/index/index.json`
- `goto-wechat/pages/index/index.wxml`
- `goto-wechat/pages/index/index.wxss`

**将被重命名的目录：**
- `goto-wechat/pages/home/` → `goto-wechat/pages/index/`

**将被修改的文件：**
- `goto-wechat/app.json` - 更新 pages 数组第一项
- `goto-wechat/pages/login/login.js` - 更新登录跳转路径

---

## Chunk 1: 文件操作与配置更新

### Task 1: 删除旧的 index 目录

**Files:**
- Delete: `goto-wechat/pages/index/`

- [ ] **Step 1: 切换到小程序项目目录**

```bash
cd /mnt/f/IdeaProjects/goto-software/goto-wechat
```

预期：成功切换到 goto-wechat 目录

- [ ] **Step 2: 使用 Git 删除旧的 index 目录**

```bash
git rm -rf pages/index
```

预期：输出类似 `rm 'pages/index/index.js'` 等信息，表示文件已从 Git 追踪中删除

- [ ] **Step 3: 验证删除结果**

```bash
ls pages/index 2>&1
```

预期：输出 `ls: cannot access 'pages/index': No such file or directory`

---

### Task 2: 重命名 home 目录为 index

**Files:**
- Rename: `goto-wechat/pages/home/` → `goto-wechat/pages/index/`

- [ ] **Step 1: 使用 Git 重命名目录**

```bash
git mv pages/home pages/index
```

预期：命令成功执行，无错误输出

- [ ] **Step 2: 验证重命名结果**

```bash
ls -la pages/index/
```

预期：显示 index.js, index.json, index.wxml, index.wxss 四个文件

- [ ] **Step 3: 验证 home 目录已删除**

```bash
ls pages/home 2>&1
```

预期：输出 `ls: cannot access 'pages/home': No such file or directory`

- [ ] **Step 4: 检查 Git 状态**

```bash
git status
```

预期：显示 `renamed: pages/home/index.js -> pages/index/index.js` 等重命名信息

---

### Task 3: 更新 app.json 配置

**Files:**
- Modify: `goto-wechat/app.json:3`

- [ ] **Step 1: 读取当前 app.json 内容**

```bash
cat app.json | head -10
```

预期：看到 pages 数组第一项是 `"pages/home/index"`

- [ ] **Step 2: 修改 pages 数组第一项**

使用 Edit 工具修改 `app.json` 文件：

```
file_path: /mnt/f/IdeaProjects/goto-software/goto-wechat/app.json
old_string: "pages/home/index"
new_string: "pages/index/index"
```

修改前：
```json
"pages": [
  "pages/home/index",
  "pages/assistant/index",
```

修改后：
```json
"pages": [
  "pages/index/index",
  "pages/assistant/index",
```

- [ ] **Step 3: 验证修改结果**

```bash
cat app.json | head -10
```

预期：看到 pages 数组第一项是 `"pages/index/index"`

---

### Task 4: 更新登录跳转逻辑

**Files:**
- Modify: `goto-wechat/pages/login/login.js:180`

- [ ] **Step 1: 读取当前登录跳转代码**

```bash
sed -n '175,185p' pages/login/login.js
```

预期：看到第 180 行包含 `url: "/pages/home/index"`

- [ ] **Step 2: 修改跳转路径**

使用 Edit 工具修改 `pages/login/login.js` 文件：

```
file_path: /mnt/f/IdeaProjects/goto-software/goto-wechat/pages/login/login.js
old_string: url: "/pages/home/index"
new_string: url: "/pages/index/index"
```

修改前：
```javascript
wx.redirectTo({
  url: "/pages/home/index",
  success: () => {
```

修改后：
```javascript
wx.redirectTo({
  url: "/pages/index/index",
  success: () => {
```

- [ ] **Step 3: 验证修改结果**

```bash
sed -n '175,185p' pages/login/login.js
```

预期：看到第 180 行包含 `url: "/pages/index/index"`

---

### Task 5: 全局引用检查

**Files:**
- Check: All files in `goto-wechat/`

- [ ] **Step 1: 搜索所有 pages/home 引用**

```bash
grep -r "pages/home" . --include="*.js" --include="*.json" --include="*.wxml" --include="*.wxss"
```

预期：无输出（所有引用已更新）或仅显示注释中的引用

- [ ] **Step 2: 如果发现其他引用，逐一更新**

如果 Step 1 发现其他引用（预期不会有，因为只有 app.json 和 login.js 需要更新），则：

1. 列出所有包含引用的文件
2. 对每个文件使用 Edit 工具，将 `pages/home/index` 替换为 `pages/index/index`
3. 如果发现意外的引用，暂停并报告给用户确认

- [ ] **Step 3: 再次验证**

```bash
grep -r "pages/home" . --include="*.js" --include="*.json" --include="*.wxml" --include="*.wxss"
```

预期：无输出

---

### Task 6: 提交变更

**Files:**
- Commit: All modified files

- [ ] **Step 1: 查看变更状态**

```bash
git status
```

预期：显示重命名的文件和修改的 app.json、login.js

- [ ] **Step 2: 添加修改的文件到暂存区**

```bash
git add app.json pages/login/login.js
```

预期：命令成功执行

- [ ] **Step 3: 提交变更**

```bash
git commit -m "$(cat <<'EOF'
refactor: 用功能列表页面替换欢迎页面

- 删除旧的 pages/index 欢迎页面
- 将 pages/home 重命名为 pages/index
- 更新 app.json 中的首页配置
- 更新登录跳转路径到新的首页
EOF
)"
```

预期：输出提交成功信息，显示变更的文件数量

- [ ] **Step 4: 验证提交**

```bash
git log -1 --oneline
```

预期：显示刚才的提交信息

---

## Chunk 2: 测试验证

### Task 7: 文件结构验证

**Files:**
- Verify: `goto-wechat/pages/index/`

- [ ] **Step 1: 确认 pages/index 目录存在**

```bash
ls -la pages/index/
```

预期：显示 index.js, index.json, index.wxml, index.wxss 四个文件

- [ ] **Step 2: 确认 pages/home 目录已删除**

```bash
ls pages/home 2>&1
```

预期：输出 `ls: cannot access 'pages/home': No such file or directory`

- [ ] **Step 3: 检查文件内容是否正确**

```bash
head -20 pages/index/index.wxml
```

预期：看到功能列表页面的内容（数据处理、统计分析、数据画图、实用工具等）

---

### Task 8: 配置验证

**Files:**
- Verify: `goto-wechat/app.json`

- [ ] **Step 1: 检查 app.json 配置**

```bash
cat app.json | head -10
```

预期：pages 数组第一项是 `"pages/index/index"`

- [ ] **Step 2: 检查 JSON 格式是否正确**

```bash
python3 -m json.tool app.json > /dev/null && echo "JSON 格式正确" || echo "JSON 格式错误"
```

预期：输出 `JSON 格式正确`

---

### Task 9: 功能验证

**Files:**
- Verify: Login flow and navigation

- [ ] **Step 1: 检查登录跳转路径**

```bash
grep -n "redirectTo" pages/login/login.js | grep "pages/index"
```

预期：显示第 179 行包含 `url: "/pages/index/index"`

- [ ] **Step 2: 手动测试建议**

在微信开发者工具中：
1. 打开 goto-wechat 项目
2. 检查编译是否成功
3. 测试登录流程
4. 确认登录成功后跳转到功能列表页面
5. 确认首页显示的是功能列表（数据处理、统计分析、数据画图、实用工具）

---

## 完成检查清单

- [ ] pages/index 目录存在且包含正确的文件
- [ ] pages/home 目录已被删除
- [ ] app.json 中 pages 数组第一项是 pages/index/index
- [ ] login.js 中登录跳转路径是 /pages/index/index
- [ ] 没有其他地方引用 pages/home
- [ ] Git 提交已完成
- [ ] 小程序可以正常编译
- [ ] 登录流程测试通过

---

## 回滚方案

如果出现问题，可以使用以下命令回滚：

```bash
# 方式 1：回滚到上一次提交
git reset --hard HEAD~1

# 方式 2：使用 reflog 找到操作前的状态
git reflog
git reset --hard <commit-hash>
```

